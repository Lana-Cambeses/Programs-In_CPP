bool LuckyArray(int* arr, int len) {
    //EXERCISE: Lucky Array is an array where sum of all odd positioned numbers equals the sum of all even positioned numbers.
    //Otherwise, not a lucky array

}